spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://era-server-database.cqdhf53pi1e5.ap-northeast-2.rds.amazonaws.com:3306/era?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: founders2021!
    hikari:
      minimum-idle: 10
      maximum-pool-size: 10

  thymeleaf:
    cache: false # default는 true. 개발 시에 false로 두면 수정 시 바로 반영.
    prefix: classpath:templates/
    check-template-location: true
    suffix: .html
    mode: HTML

  jpa:
    database-platform: org.hibernate.dialect.MySQL57Dialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        use_sql_comments: true